This site can be rendered with jekyll (http://github.com/mojombo/jekyll)

Once you have installed jekyll, run the following command and you should see the website on http://localhost:4000

$ jekyll --server --auto
